# Configurações do Banco de Dados (Turso / LibSQL)
# Para usar o banco local (padrão), deixe em branco ou use: file:burger_manager.db
DATABASE_URL=
DATABASE_AUTH_TOKEN=

# Chave secreta para autenticação (JWT)
JWT_SECRET=burger-manager-super-secret-key
